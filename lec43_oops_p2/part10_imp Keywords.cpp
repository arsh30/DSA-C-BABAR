/*
1) Static Keyword ->  When a variable is declared as static, space for it gets allocated for the
lifetime of the program. Even if the function is called multiple times, space for the static variable is allocated only once
and the value of variable in the previous call gets carried through the next function call.


2) Final Keyword -> When a variable is declared with the final keyword, its value can’t be modified, essentially, a constant.

3) This Keyword => It is pointer used to point the current Object and It stores the address the address of that object

4) New Keyword => new keyword is used to create an instance of the class. variable create
                in Heap , It allocate memory at run time


5) Super Keyword =>
*/